@model IEnumerable<FPTBookstoreApplication.Models.Order>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminPage.cshtml";
}

<h2 style="margin: 3% 0 5% 45%; font-style:italic; color:chocolate">List of Orders</h2>

<table class="table table-striped table-dark">
    <tr style="text-align:center">
        <th>
            @Html.DisplayNameFor(model => model.OrderId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Account.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Account.PhoneNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalPrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Addressdilivery)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr style="text-align:center;">
            <td>
                @Html.DisplayFor(modelItem => item.OrderId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Addressdilivery)
            </td>
            <td>
                @Html.ActionLink("Delete", "DeleteOrder", new { id = item.OrderId }, new { onclick = $"return confirm('Are you sure to detele order of:{item.UserName}?')" })
            </td>
        </tr>
    }

</table>
